# dsh-pretext profile patch — insert one loader entry for the plugin package.
# The browser half is picked up by dsh-client-modules through the package's
# dsh.client declaration; the host half is a no-op.
- insert:
    - id: dsh-pretext
      name: 'dsh-pretext'
